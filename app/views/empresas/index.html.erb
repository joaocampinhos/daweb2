<div class="container">
  <div class="fullwidth">
    <div>
      <form action="./search.html" method="get">
        <input name="company" type="search" class="search" placeholder="Pesquisa">
      </form>
    </div>
    <div>
      <h2 class="title">Empresas</h2>
      <% @empresas.each do |empresa| %>
        <div class="company">
          <h3><%= link_to empresa.name, empresa %></h3>
          <p><%=h truncate(empresa.description, :length => 100) %></p>
          <p class="func"><%=empresa.empregados.count%> Funcionários</p>
        </div>
      <% end %>
    </div>
  </div>
</div>
